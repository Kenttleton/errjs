/**
 * The Network module monitors HTTP and AJAX calls and outputs to the console
 *      verbose errors
 * @module Network
 */
const Network = (function(Network) {
  const init = function() {};
  return {init};
})(Network, undefined);
